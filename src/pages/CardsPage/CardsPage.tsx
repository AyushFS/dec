import React from 'react';
import Cards from '../../features/Cards/Cards';

function CardsPage() {
	return (
		<div className="cards-page" data-testid="cards-page">
			<Cards />
		</div>
	);
}

export default CardsPage;
