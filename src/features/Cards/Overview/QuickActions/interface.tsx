import { IconTypes } from '../../../../components/FsIcon/constants';

interface QuickActionConfig {
	icon: React.ReactNode;
	iconType?: IconTypes;
	title: string;
	path: string;
}

export default QuickActionConfig;
