export enum ButtonTypes {
	'button' = 'button',
	'submit' = 'submit',
	'reset' = 'reset',
}
export enum ButtonSizes {
	'default' = 'default',
	'small' = 'small',
	'large' = 'large',
}
export enum ButtonColors {
	'primary' = 'primary',
	'secondary' = 'secondary',
}

export const BUTTON_SIZES = ['default', 'small', 'large'];
export const BUTTON_COLORS = ['primary', 'secondary'];
export const BUTTON_TYPES = ['button', 'submit', 'reset'];
